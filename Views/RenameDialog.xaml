<Window x:Class="WinAuthRemaster.Views.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Rename_Title}" Width="360" Height="180"
        Background="{DynamicResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{DynamicResource Rename_Name}"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   FontSize="12" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="NameInput"
                 Style="{StaticResource ModernTextBox}" Margin="0,0,0,8"
                 MaxLength="25"
                 KeyDown="OnKeyDown"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Btn_Cancel}" Style="{StaticResource ModernButton}"
                    Click="OnCancel" Margin="0,0,8,0" Width="80"/>
            <Button Content="{DynamicResource Btn_OK}" Style="{StaticResource PrimaryButton}"
                    Click="OnOk" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
