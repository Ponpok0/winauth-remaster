<Window x:Class="WinAuthRemaster.Views.ConfirmDeleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Delete_Title}" Width="380" Height="240"
        Background="{DynamicResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="MessageText"
                   Foreground="{DynamicResource DangerBrush}"
                   FontSize="14" FontWeight="SemiBold"
                   TextWrapping="Wrap" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   FontSize="12" Margin="0,0,0,4">
            <Run Text="{DynamicResource Delete_TypeToConfirmPrefix}"/>
            <Run Text=" "/><Run Text="{DynamicResource Delete_TypeToConfirmKeyword}" FontWeight="Bold"/>
            <Run Text=" "/><Run Text="{DynamicResource Delete_TypeToConfirmSuffix}"/>
        </TextBlock>

        <TextBox Grid.Row="2" x:Name="ConfirmInput"
                 Style="{StaticResource ModernTextBox}" MaxLength="20" Margin="0,0,0,8"
                 TextChanged="OnTextChanged" KeyDown="OnKeyDown"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Btn_Cancel}" Style="{StaticResource ModernButton}"
                    Click="OnCancel" Margin="0,0,8,0" Width="80"/>
            <Button x:Name="DeleteButton" Content="{DynamicResource Btn_Delete}"
                    Style="{StaticResource ModernButton}"
                    Foreground="{DynamicResource DangerBrush}"
                    Click="OnDelete" Width="80" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>
